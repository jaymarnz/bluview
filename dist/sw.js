////////////////////////////////////////////////////////////////////////////////////
// In order to install as a PWA a service worker must exist
// This is the minimal worker which fetches everything from the network
self.addEventListener('fetch', function () {
  return;
})
